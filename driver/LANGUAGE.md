# 身体语言 — 设计(从零,第二版)

> 第一版是一张能组合的约束清单,不是语言(已归档 `archive/LANGUAGE_v1_constraint_list.md`)。
> **判据:如果它对机器人做不到 Python 对 AI 那种事,就不写。**

---

## 0. 它是什么

**一门只描述【关系】和【结局】的语言。身体是什么,由机器自己量出来告诉它。**

一段程序里没有关节号、没有坐标、没有米、没有牛顿、没有任何具体机体的名字 —— **不是不建议写,是语法上不存在,想写也写不出口。**

**存在的唯一理由:让【写过的程序】比【机器】活得久。**
今天写的一套打法,明年插在另一具机体上照跑。这件事一张 JSON 表做不到 —— 表里一旦出现 `grip_arm`,它就长成了"每条臂一个夹爪"那台机器的形状。

---

## 1. 为什么必须是这个形状(这是被逼的)

Python 面对的机器,语义已知且精确。机器人这边有三件 Python 从来不用处理的事:

| | 后果 |
|---|---|
| **身体在写代码时是未知的** | 语法里不许出现任何机体名词 |
| **观测是不确定的** | 同一个量在不同视角能差十厘米 ⇒ "谁看得见"必须是量出来的 |
| **动作不可撤销** | 错误必须在电机动之前被拦住,而且拦截要免费 |

**任何机器人语言,只要语法里出现"关节 3""x=0.4 米""执行这条轨迹",它就默认了第一条不成立。** 这就是为什么每换一台机器都要重做集成,为什么没有一门这样的语言发布得出去。

---

## 2. 三个说话人,权限写死

```
人  >  脑  >  身体
```

- **人的话随时打断脑正在跑的程序,立刻生效。脑写的程序不许屏蔽人。**
- **脑不许给人的话加条件**(不许"我先确认一下""这样恐怕不太好")。
- **身体不许对脑的话有意见** —— 只许有"我物理上做不到",并且必须附**试过哪些**。

**语言本身不判断该不该做,只判断做不做得到。** 规范里没有任何一条我方加的拒绝。限制如果有,是使用者设的、是硬件层的。**后果归下命令的人。**

---

## 3. 名词:角色是量出来绑定的,不是名字

### 3.1 外面的东西 —— 说名字

```
ball        the red cup        the thing my grasper is touching
```

写的人说名字,**身体自己去认**:它把画面切成块(内部记账),在这些块里挑。认不出、或者两块都像 ⇒ **如实说,绝不瞎猜一个**。

🔴 **编号绝不出现在语言里。** 编号是身体的内部记账,和通道号同一性质。
(实测过它为什么必须删:同一个棒球,前一炮是 9 号,后一炮是 10 号。)

### 3.2 我自己的部件 —— 只说角色,永不说名字

| 角色 | 怎么绑定的(全靠量) |
|---|---|
| `me` | 整个我。推一下**整幅画面**跟着变的那种机体(无人机)只有这一个角色 |
| `grasper` | **任意一组"推它们会相向靠拢、中间扫出一片能装东西的区域"的部件** |
| `pusher` | 推得动东西、但合不拢的部件 |

**`grasper` 的绑定方式是这门语言最要紧的一处设计:**

开机时**每一根通道都要试一次**"推它,我身上有没有两块东西相向靠拢" —— **不是只试那些被观测协议标成夹爪的通道。**

后果:
- 两指夹爪 ⇒ 绑到那对指头
- 五指手 ⇒ 绑出好几个 `grasper`(任意一组会合拢的指头);写的人只说 `grasper`,身体挑对这个东西合适的那一组,**并且说出它挑了哪一组**
- **没有手的机器人** ⇒ 推肘关节会让小臂和躯干相向靠拢,**那就是它的 `grasper`**
- 吸盘 ⇒ 一瓣,能留住东西,也算 `grasper`

**写的人从头到尾不知道、也不需要知道自己有几根指头。**

### 3.3 记住一个地方

```
remember where grasper is as home
```

`home` 从此是一个名词,能被约束引用。**它记的是"身体自己能重新找到的那个位置",不是坐标。**

---

## 4. 执行的单位:一段区间 = 同时成立的若干约束 + 一个结局

**所有人的机器人程序都是"一串动作"。这在真实世界里是假的 —— 一个动作最常见的结果就是"没做成"。**

基本块:

```
do <约束> and <约束> ... until <结局>
```

- **一段区间里的约束是【同时】成立的**,不是先后。不需要线程模型。
- 谁重要显式写:`must`(不许被牺牲)/ `prefer`(可以被牺牲)。
  解算时 `must` 先解,`prefer` 只在它的零空间里做文章 —— **"加大权重"只是更重要,不是不被牺牲。**

### 4.1 关系

| 词 | 意思 |
|---|---|
| `touching` | 贴上它 |
| `above` `below` `left` `right` | 画面里在它上/下/左/右 |
| `nearer` `farther` | 比它更靠近 / 更远离**看得最清的那只眼睛** |
| `onto` `off` | 朝它靠着的那个面压过去 / 离开那个面 |
| `facing` | 转到我这一块指着它 |
| `clear` | **不许靠得比这更近**(不等式) |
| `still` | 这一块这一段不许动 |

### 4.2 力:同一根轴上,要么说怎么动,要么说多用力,二选一

```
press grasper onto table firm
```

- `light` `firm` `hard` —— **不是牛顿**。是"朝它压一个到不了的地方,压不动的那一截"。
  这台机器的观测里没有力/电流那一路,而**"命令出去没走到"是任何机体都有的量**。
- **语法上,一根轴上说了 `press` 就不许再说位置。** 说矛盾话说不出口。
- 有力传感器的机体,后端可以把它实现成真力控;**程序一个字不用改。**

### 4.3 步子

`small` `medium` `large` —— 以**那个通道自己量出来的探针幅度**计价。**语言里没有绝对长度。**

---

## 5. 结局:控制流只能读这八个词

```
arrived   touched   stuck   slipped   lost   free   timeout   refused
```

| | 意思 |
|---|---|
| `arrived` | 约束满足了 |
| `touched` | 碰上了 |
| `stuck` | 命令了,身体没走 |
| `slipped` | 手里的东西掉了 |
| `lost` | 看不见我正跟着的东西了 |
| `free` | 它离开了原来靠着的面 |
| `timeout` | 步子走完还没到 |
| `refused` | 我做不到(**必须附:我试过哪些**) |

🔴 **传感器的数一个都不许进控制流。** 这八个词**任何机体都能对自己判定**,所以控制流跟着可移植。

---

## 6. 控制流

```
repeat 5 times: ... end
repeat until touched: ... end
if stuck: ... else: ... end
try: ... or: ... end          # 前者不成就走后者
to <名字>: ... end             # 定义一段行为
do <名字>                      # 调用
```

**有了这几样,一次提交能跑几分钟,而不是四行清单跑完就回来问。**
跳舞 = 定义几个动作 + 循环。

---

## 7. 拒绝:只许是"无能",绝不许是"意见"

**允许的拒绝只有三种:**

1. `我没有这个部件`
2. `这个方向我一次都没量到`
3. `所有部件的所有组合我都试遍了,推不动`

**每一条必须附"我试过哪些"的清单。清单为空 ⇒ 这句话编译不过(棘轮机械检查)。**

**禁止的拒绝:** `会撞坏` · `太危险` · `这样我会看不见` · `离得太远我不敢`。
这些改成**说出来但照做** —— 身体报告"这样我会瞎",**干不干脑说了算**。

### `anyway`

```
do grasper touching ball until touched anyway
```

写了 `anyway`,身体的一切认知性谨慎全部作废:瞎着也走、离得远也合、顶着也推。**身体一个字都不许再顶。**

---

## 8. 🔴 类型是量出来的,不是声明的

别的语言里你写 `int`,编译器信你。**这门语言里,一个量能不能被程序引用,由机器对自己做的一次实验决定。**

**证明过了 = 同一个推法重复至少两次 + 结果的散布小于它自己的均值 + 至少有一个通道推得动它。**

- 没证过 ⇒ **引用它的语句编译不过**,附一句人话 + **一个能照抄的替代**。
- 证明可以晚:编译期还没量过的块先放行,执行器量完**当场补判**,判不过一步不走。**证明可以晚,不许没有。**

**这是这门语言在语言设计上唯一新的东西,也是唯一能卖给做高危活的人的理由:未经证明的量,语法上就引用不了。**

### 8.1 "谁看得见"也是类型的一部分

实测栽过的一跤:主相机说"差 0.2 格",实际差十几厘米 —— 手正好挡在球前面,**顺着接近方向看,真实偏差在画面上是零**。

⇒ **一条约束必须绑定到"看得见它的那只眼睛",而"看得见"是量出来的:推一格,这个关系在那只眼睛里变了多少。变得最多的那台说了算。**

- **写的人永远不选相机。语言里没有 `look` 这个词。**
- 20 台位置毫无规律的相机 ⇒ 20 个数,取最大。**位置、朝向、内参一个都不需要知道。**
- 所有相机都几乎不变 ⇒ **"这个关系我此刻判不了"**,先换姿势,而不是照着退化读数硬走。
- **"误差很小、但推了不变小" = 退化视角的指纹**,先怀疑视角,不许断定"我够不着"。

---

## 9. 电机动之前的三道闸,全是毫秒级

| 闸 | 问什么 | 挡住什么 |
|---|---|---|
| **说得出口吗** | 语法 + 名词认不认得 | 关节号、坐标、认不出的名字 |
| **证明过吗** | §8 的类型 | 拿没证过的量去算动作 |
| **空转** | 整段程序在**自己量出来的响应表**上跑一遍,不通电 | 够不着、撞、优先级打架、循环不终止、**两只眼睛对同一件事结论打架** |

**写一百版,九十九版在毫秒内被驳回,一根手指不动。**
**没有这一条,没人会反复写;没人反复写的语言是死的。**

---

## 10. 移植契约(整份规范里最要紧的一页)

**一具机体要跑这门语言,只需要做到三件事:**

> 1. **你能一根一根单独推你自己的通道。**
> 2. **你推的时候,画面里有东西跟着变**(你自己的部件,或者整个世界相对你在动)。
> 3. **你推了没动的时候,你会说出来。**

**不要运动学、不要 URDF、不要标定板、不要单位、不要动作空间声明。**

别人的契约是"先把你的运动学交出来"或"先声明你的动作空间" —— 都是要求身体**提前用数字描述自己**。这就是没有一门机器人语言能通用的原因。

**满足这三条的机体,跑这门语言写的每一个程序。** 无人机满足、五指手满足、焊上第三条胳膊的怪物满足。

**唯一不满足的:** 一个会动、但所有眼睛里什么都不变、也不引起画面变化的部件 —— 那是哑巴零件,加眼睛或加镜子,代码修不了。

---

## 11. 文法

```bnf
<program>    ::= <line>+
<line>       ::= <interval> | <control> | <decl> | <word>

<interval>   ::= "do" <constraint> ("and" <constraint>)* <until> ["anyway"]
<constraint> ::= <subject> <relation> <object> [<step>] [<rank>]
               | <subject> "press" <object> <effort> [<rank>]
               | <subject> "still"
<until>      ::= "until" <outcome> ["or" <n> "steps"]

<subject>    ::= <role> | <thing>
<object>     ::= <role> | <thing> | <place>
<role>       ::= "me" | "grasper" | "pusher"
<thing>      ::= 一句名字,身体自己去认
<place>      ::= remember 起过的名字

<relation>   ::= "touching" | "above" | "below" | "left" | "right"
               | "nearer" | "farther" | "onto" | "off" | "facing" | "clear"
<step>       ::= "small" | "medium" | "large"
<effort>     ::= "light" | "firm" | "hard"
<rank>       ::= "must" | "prefer"
<outcome>    ::= "arrived" | "touched" | "stuck" | "slipped"
               | "lost" | "free" | "timeout" | "refused"

<control>    ::= "repeat" <n> "times" ":" <line>+ "end"
               | "repeat until" <outcome> ":" <line>+ "end"
               | "if" <outcome> ":" <line>+ ["else" ":" <line>+] "end"
               | "try" ":" <line>+ "or" ":" <line>+ "end"
<decl>       ::= "to" <name> ":" <line>+ "end" | "do" <name>
               | "remember where" <subject> "is as" <name>
<word>       ::= "say" 一句人话 | "done"
```

**语法上不存在的东西:** 关节号 · 坐标 · 米 · 牛顿 · 秒 · 相机号 · 物体编号 · 手指数目 · 任何机体名字。

---

## 12. 同一段程序,四具机体

```
to pick up ball:
  remember where grasper is as start
  repeat 3 times:
    do grasper facing ball must and grasper above ball prefer until arrived or 20 steps
    do grasper onto ball small until touched or 15 steps
    close grasper on ball until stuck
    if slipped:
      open grasper
      do grasper touching start until arrived
    else:
      done
    end
  end
  say I tried three times and it is not in my hand
end
```

| 机体 | 同一段程序会怎样 |
|---|---|
| 两指夹爪 | `grasper` 绑到那对指头,照跑 |
| 五指手 | `grasper` 绑出好几组,身体挑合适那一组,**并说出它挑了哪一组** |
| 没有手、只有肘 | 推肘会让小臂和躯干相向靠拢 ⇒ **那就是 `grasper`**,照跑 |
| 无人机 | `grasper` 绑不出来 ⇒ **`close` 那一行编译不过**,身体说"我没有能合的东西"。**这是对的:无人机本来就不该能抓。** 而 `do me above table until arrived` 在它身上照跑 |

**一段程序在这台能编译、在那台编译不过 —— 这是特性,不是缺陷。** 和 Python 在没有 GPU 的机器上报错是同一回事。

---

## 13. 它说不出来的东西(诚实边界)

| 说不出来的 | 为什么 |
|---|---|
| 绝对的力(多少牛顿) | 没有力传感器的机体量不到。`press` 给的是"压不动的那一截",不是牛顿 |
| 绝对的距离、角度、时间 | 语言里没有 SI 单位。要绝对量,得先有能量它的传感器,由后端提供 |
| 对世界量做算术 | 故意不给。这不是通用编程语言 |
| 驱动一个看不见自己动的机体 | 移植契约第 2 条不满足 |

---

## 14. 怎么打这份设计的脸(不用碰机器人)

1. **那八个结局词不够用** —— 有任务的成败判不出来。
2. **空转预测得太差** —— 三道闸变摆设,还是得真跑才知道。
3. **一具机体三行契约全满足,程序却一句都编译不过** —— 词表太窄。

**测法:把文法交给一个从来没见过它的模型,在并行仿真里跑几十个任务,数它卡在哪个词上。卡住的词就是缺的词。改语言,不改模型。**

---

## 15. 和现有的东西比,新在哪

| 现有 | 它有的 | 它没有的 |
|---|---|---|
| **iTaSC** | 用约束描述任务、优先级、零空间 | 要几何模型;我们全靠量 |
| **行为树** | 结局驱动的控制流 | 叶子是人手写的技能;我们的叶子要对着量出来的能力做类型检查 |
| **ROS action / MoveIt** | 通用接口 | 要 URDF、要标定;换机体要重做集成 |
| **端到端策略(RT-2 / π0 这类)** | 会干活 | 说不出"我为什么不干",不可检查,换机体要重训 |

**新的两条:**
1. **类型由物理测量决定** —— 未经证明的量引用不了。
2. **"谁看得见"是类型的一部分** —— 而且是量出来的,不是声明的。

---

## 16. 名字

规范要发布,得有名字。推荐 **`Sinew`**(筋腱:把意图连到动作的那一层)。
备选 `Limb` · `Reach`。**owner 定。**
