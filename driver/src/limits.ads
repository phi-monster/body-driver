--  纯粹的容量上限:给定长数组一个够用的边界。这里【不许】出现任何身体量 ——
--  实际有几条臂、几个抓握通道、几台相机,全都是量出来的,只有数组的天花板写在这儿。
--  单独一个包是为了断开 Plug → Layout → Chan → Plug 的循环依赖(它谁也不依赖)。
package Limits is
   --  一条臂上最多几个抓握通道(次数,无量纲):两指手 1 个,五指手 5 个,留点余量。
   Max_Jaws : constant := 8;
end Limits;
