# body layer — 身体层驱动

> **世界靠学,身体靠量。我一动,跟着动的那一块,就是我。手在哪,靠感觉不靠认:关节这样的时候手在画面哪儿,看过一次就记住。**
> 脑(大模型)只看带编号的画面、只说"第几号 → 去哪 → 直到什么事件为止";身体靠自己动一动把一切量出来;两边之间没有第三种东西。

## 在做什么(边界)
| 学的 | 量的 | 不做 |
|---|---|---|
| 世界:脑在编号彩图上点名(`driver/src/brain.adb` 合同,`问脑` 同款) | 部件图(逐通道推)· 相机长在哪只手 · 握区(合空扫过的像素)· 通道响应表(递推最小二乘)· 噪声地板 · 碰上/推不动(零表更准)· 拿住了没(抬一截看跟不跟) | 机体参数 / URDF / 相机内外参 / 写死的策略 / 真机 / 力的大小(只当机体自报的数据) |

驱动 = `driver/`(Ada;监视器 `monitor` 与备份 `backup` 是 SPARK,gnatprove 34 条全证;其余 Ada 加运行时检查)。零依赖(msgpack、WebSocket、HTTP、JSON、BMP、光流全自写)。安全核 `fast/`(SPARK,40/40)沿用。
**身体图**(`schema`,owner 2026-09-07:"人永远不需要认出自己的手"):每次真看见自己手指(合手/抖手指时动过的像素)就把 位姿 → 两瓣位置+深度 记成样本;之后手在世界相机里的位置 = 按此刻位姿查最近样本 + 响应表外推,**不看画面**;熟地(离样本不超过一步核实过的步幅)只让光流核对,生地或大步才抖手指看一眼、看完记进图。存进身体文件。
**全身,只有一种东西:零件**(owner 2026-09-07:"必须加了全身""手指不也是零件吗"):每个通道带一块 —— 那个通道单独动时跟着动的那一块(从那个关节往外);握合通道也是通道,它带的那块就是手指(`Chan_K = Chan.Per_Arm`),多团时每团一个点(两指 = 2 团;五指是欠账)。开机每个通道推过一下、合空一次,就把全部零件记进同一位姿的样本;脑能点名任何一块并让它去某格/某关系,身体缺表当场量表;生地/大步后"看一眼" = 推一下它自己的通道再推回(手指就是合几拍再张回),动过的像素就是它。
**越用越强 = 身体文件**(`--in/--out cal.json`,钥匙 = 机体自报的形状):开机装回 → 每只手推一下核对 → 同一具身体直接用(探针 300 拍 → 核对 ~20 拍);对不上才重量;历次量到的取中位数、噪声地板只放大;响应表和核实过的步幅一起存。

## 现在在哪(2026-09-13 晚)
- 官方 `general_pickup`:**0/55**。**新语言 Sinew 与旧的两数字接口,成绩完全一样,都是 0/55。**
- 🔴 **诚实的账:语言到今天是净负债。** 旧接口那三炮(FM/FN/FO)**连着三炮手都摸到球上并合了爪**;
  用 Sinew 的 GM **连球边都没到**。而且**语言的新本事一次都没在真机上用过** —— GM 全程只用
  `do grasper touching X until touched or N steps`,这一句旧接口也说得出("第几号 → 到第几格")。
  退步的两笔账:①语言那层在撒谎(4 个假结局词 / 1 个假角色 / 写的步数被丢 / `until arrived` 只走一步,**已全修**)
  ②更大一头是 `a7ab7e9` 那次回滚把"近处不瞎"一类修法一起退掉了(**已捞回**)。
- **GM 的死因链(逐环有读数)**:手一凑近 ⇒ 球(3027 px)和乐高(5098 px)双双从可见清单消失,只剩 2 米外
  10 px 的墙斑 ⇒ 模板飘到 **2.13 m** 外的墙上 ⇒ 追着墙把关节顶死,连着 60 步实到精确 **0.0000** ⇒ 驱动崩溃。
  全程只跟**一个**中心点 ⇒ 手腕自转没人管。
- **今天砍完、装上箱、未打过的 18 条**(逐条见 `LAB.md` 2026-09-13):
  1. **近处不瞎**:第二把尺子用那东西自己的宽度(`C.Want_Size`,量出来的);长着胳膊的相机里贴边的块不许丢。
     **在造出来的深度图上离线验过会响**(小尺子下大块确实切不出 / 换尺子就回来 / 贴边那条单独验)。
  2. **接触点回来**:每一瓣一个点,瓣数读身体量到的数;**手自己的相机里也展开**(以前那段代码在 GM 里从没执行过)。
  3. 语言的四个假结局词、一个假角色全修;`until arrived` 不再一步就停;命令了没动**加大**步子(以前是缩,缩到底永久死锁);
     重连后帧数相减为负导致的崩溃修好。
  4. 每一步报**米**(`差 0.084 m`),可直接和 FO 的 8.4 cm 对上。
- **语言焊缝 14 条**,每条都做过"故意改坏 ⇒ 该红"。**其中我自己写出过两条永不失败的断言**,都是这一步逮出来的。
- **每台相机都编号了**(`98a178d`),**一段动作跟着被点名那个东西所在的相机走**(`0cd14c7`) ——
  GM 里球从手腕相机消失而头顶相机一直看得见、身体却从没去那儿看,这条堵的就是它。
  代价照记:清单跨相机立刻带出 4 处静默 bug(槽号和画面坐标都是"每台各一套"),已扫完(`496065a`)。
- 欠账:`Hand_Zone` 只有 A/B 两瓣(`Zone`/`Schema`/身体文件三处还写死两瓣)· "东西"仍绑在某台相机的第几块上,
  不是世界里的一个东西 · 一段动作仍只跑在一台相机里,别的相机的图**没有编号**、脑点不了名 · 语言里没有"去找"。
- **起炮要 owner 点头。**

## 下一步与判据
| 炮 | 赢 | 输 |
|---|---|---|
| GN(带 `BL_CUTLOG=1`,`CFG=x5_grasp` + `DRVMODE=work`,`ROBODOJO_RUN_ID=GN`) | ① 手凑近后球**还在清单里**(`BL_CUTLOG` 印:窗口 px · 切出几块 · 最宽一块 px)② 一段能走满脑写的步数 ③ 手到球报**米**,和 FO 的 0.084 m 直接比 ④ 球离开桌面在两指之间,**手腕相机 + 不动的相机各一张图** | 又消失 / 又是 1 推 timeout / 撞飞 / 合空 / 假拿住 —— 每条都要有落图和日志证据 |
| 🔴 **语言什么时候才算挣到钱** | 下一炮的命令必须是**旧接口说不出来的话**:一个回合里 `try: 抓 or: 退开重来 end`,中间**不回来问脑** | 做不成,账就是账 |

## 怎么跑
```
bash install.sh                 # 三道棘轮 → 自检 → 证明 → 编译 → 装到 ~/.local/bin/bl-calibrate
bl-calibrate --listen 9080 --eye 127.0.0.1:8079   # 脑桥在 :8079;BL_ORDER 覆盖任务句;BL_LOOK=1 只看不动;BL_DUMP/BL_VID 落图
```
陷阱与逐炮读数见 `LAB.md`;三份自由搜索报告与通读笔记在 `results/refactor_sep2026/`。
