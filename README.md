# body layer — 身体层驱动

> **世界靠学,身体靠量。我一动,跟着动的那一块,就是我。手在哪,靠感觉不靠认:关节这样的时候手在画面哪儿,看过一次就记住。**
> 脑(大模型)只看带编号的画面、只说"第几号 → 去哪 → 直到什么事件为止";身体靠自己动一动把一切量出来;两边之间没有第三种东西。

## 在做什么(边界)
| 学的 | 量的 | 不做 |
|---|---|---|
| 世界:脑在编号彩图上点名(`driver/src/brain.adb` 合同,`问脑` 同款) | 部件图(逐通道推)· 相机长在哪只手 · 握区(合空扫过的像素)· 通道响应表(递推最小二乘)· 噪声地板 · 碰上/推不动(零表更准)· 拿住了没(抬一截看跟不跟) | 机体参数 / URDF / 相机内外参 / 写死的策略 / 真机 / 力的大小(只当机体自报的数据) |

驱动 = `driver/`(Ada;监视器 `monitor` 与备份 `backup` 是 SPARK,gnatprove 34 条全证;其余 Ada 加运行时检查)。零依赖(msgpack、WebSocket、HTTP、JSON、BMP、光流全自写)。安全核 `fast/`(SPARK,40/40)沿用。
**身体图**(`schema`,owner 2026-09-07:"人永远不需要认出自己的手"):每次真看见自己手指(合手/抖手指时动过的像素)就把 位姿 → 两瓣位置+深度 记成样本;之后手在世界相机里的位置 = 按此刻位姿查最近样本 + 响应表外推,**不看画面**;熟地(离样本不超过一步核实过的步幅)只让光流核对,生地或大步才抖手指看一眼、看完记进图。存进身体文件。
**全身,只有一种东西:零件**(owner 2026-09-07:"必须加了全身""手指不也是零件吗"):每个通道带一块 —— 那个通道单独动时跟着动的那一块(从那个关节往外);握合通道也是通道,它带的那块就是手指(`Chan_K = Chan.Per_Arm`),多团时每团一个点(两指 = 2 团;五指是欠账)。开机每个通道推过一下、合空一次,就把全部零件记进同一位姿的样本;脑能点名任何一块并让它去某格/某关系,身体缺表当场量表;生地/大步后"看一眼" = 推一下它自己的通道再推回(手指就是合几拍再张回),动过的像素就是它。
**越用越强 = 身体文件**(`--in/--out cal.json`,钥匙 = 机体自报的形状):开机装回 → 每只手推一下核对 → 同一具身体直接用(探针 300 拍 → 核对 ~20 拍);对不上才重量;历次量到的取中位数、噪声地板只放大;响应表和核实过的步幅一起存。

## 现在在哪(2026-09-13)
- 官方 `general_pickup`:**0/55**(一次都没抓起来过)。最近一炮 **GM**:图 5 张 · 命令 8 条 · **抓起 0 次**,球全程没被碰动。
- **GM 的唯一好消息**:Sinew 端到端跑通了(解析 → 按测量绑角色 → 三道闸 → 运行时 → 结局回喂脑),而且唯一真正走起来的那一段 60 推里,手到球的差距 **0.588 → 0.262** —— **这具身体第一次是在靠近,不是在变远**。
- **GM 怎么死的**:手冻死 60+ 步(命令发出去、关节实际转动量精确 0.0000)⇒ 驱动崩溃 `CONSTRAINT_ERROR act.adb:1518`。
- 🔴 **GM 暴露的是一整类病:语言收下你的词,做的是别的词的事,还回报得一本正经。**
  五类词逐个审完 —— **结局词 9 个里 4 个假**(`lost`/`refused` 被并进"走够步数";`free` 和 `slipped` 同一个行为,而 `free` = 「它离开了原来靠着的面」= **被拿起来了**,正是本任务的判据)· **角色词 1 个假**(`pusher` 把爪心也收了,和 `grasper` 绑到同一块)· **关系词 / `press`+`effort` / `must`+`prefer` 三类干净,不用改**。
- **13 条修已装上箱、`alr build` 过、自检全过、四条棘轮全绿 —— 一条都没在真身上跑过。** 逐条见 `LAB.md` 2026-09-13。
- **语言焊缝 8 条**(`selfcheck`),每条都做过"故意改坏 ⇒ 该红"的验证。其中第一版有一条是**永不失败的断言**(比的是期望表自己跟自己),已改。
- 🔴 **唯一没修的**:手一凑近,球(3027 px)和乐高人(5098 px)双双从可见清单里消失,只剩 2 米外 10 px 的墙斑。两个可疑处已插桩(`BL_CUTLOG=1`),**要一炮才能定论**。
- 🔴 **步数账**:官方 `step_lim = 200`;箱上仍是 100000,**至今没按官方步数跑过**。owner 定(2026-09-07):先抓起来,不到 200 停;身体只数不停,拍数不进问脑的话。
- 欠账:五指手的抓握(现在只到"每个握合通道一块")· 颜色切块只在深度一块都切不出时才接手 · 一块超过两团时只存前两团 · `me` 这个角色在这具身体上绑不上(设计如此,但**这套语言因此还上不了无人机**)。
- **起炮要 owner 点头。**

## 下一步与判据
| 炮 | 赢 | 输 |
|---|---|---|
| GN(带 `BL_CUTLOG=1`,`BL_ORDER` + `CFG=x5_grasp` + `DRVMODE=work`,`ROBODOJO_RUN_ID=GN`) | ① 一段能真的走满脑写的步数(不再一步 timeout)② 命令了没动时身体**加大**步子并说出来,不再冻死 ③ `BL_CUTLOG` 给出球消失的定论:是被闭运算填平,还是被判成"我自己"丢掉 ④ 最终:球离开桌面在两指之间,**手腕相机和不动的相机各一张图** | 又是每段 1 推 / 又冻死实到 0 / 又崩 —— 每条都要有落图和日志证据 |

## 怎么跑
```
bash install.sh                 # 三道棘轮 → 自检 → 证明 → 编译 → 装到 ~/.local/bin/bl-calibrate
bl-calibrate --listen 9080 --eye 127.0.0.1:8079   # 脑桥在 :8079;BL_ORDER 覆盖任务句;BL_LOOK=1 只看不动;BL_DUMP/BL_VID 落图
```
陷阱与逐炮读数见 `LAB.md`;三份自由搜索报告与通读笔记在 `results/refactor_sep2026/`。
