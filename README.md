# body layer — 身体层驱动

> **世界靠学,身体靠量。我一动,跟着动的那一块,就是我。手在哪,靠感觉不靠认:关节这样的时候手在画面哪儿,看过一次就记住。**
> 脑(大模型)只看带编号的画面、只说"第几号 → 去哪 → 直到什么事件为止";身体靠自己动一动把一切量出来;两边之间没有第三种东西。

## 在做什么(边界)
| 学的 | 量的 | 不做 |
|---|---|---|
| 世界:脑在编号彩图上点名(`driver/src/brain.adb` 合同,`问脑` 同款) | 部件图(逐通道推)· 相机长在哪只手 · 握区(合空扫过的像素)· 通道响应表(递推最小二乘)· 噪声地板 · 碰上/推不动(零表更准)· 拿住了没(抬一截看跟不跟) | 机体参数 / URDF / 相机内外参 / 写死的策略 / 真机 / 力的大小(只当机体自报的数据) |

驱动 = `driver/`(Ada;监视器 `monitor` 与备份 `backup` 是 SPARK,gnatprove 34 条全证;其余 Ada 加运行时检查)。零依赖(msgpack、WebSocket、HTTP、JSON、BMP、光流全自写)。安全核 `fast/`(SPARK,40/40)沿用。
**身体图**(`schema`,owner 2026-09-07:"人永远不需要认出自己的手"):每次真看见自己手指(合手/抖手指时动过的像素)就把 位姿 → 两瓣位置+深度 记成样本;之后手在世界相机里的位置 = 按此刻位姿查最近样本 + 响应表外推,**不看画面**;熟地(离样本不超过一步核实过的步幅)只让光流核对,生地或大步才抖手指看一眼、看完记进图。存进身体文件。
**全身**(owner 2026-09-07:"必须加了全身"):身体图不只手指 —— 每个马达单独动时跟着动的那一块 = 它带的零件(从那个关节往外),开机每个通道推过一下就记进同一位姿的样本;脑能点名任何一块(`a piece of you: everything that swings when channel k of arm A moves`)并让它去某格/某关系,身体缺表当场量表;生地/大步后"看一眼" = 手指抖一下、零件推一下自己的通道再推回,动过的像素就是它。
**越用越强 = 身体文件**(`--in/--out cal.json`,钥匙 = 机体自报的形状):开机装回 → 每只手推一下核对 → 同一具身体直接用(探针 300 拍 → 核对 ~20 拍);对不上才重量;历次量到的取中位数、噪声地板只放大;响应表和核实过的步幅一起存。

## 现在在哪(2026-09-07)
- 官方 `general_pickup`:**0/55**。新驱动真抓 3 炮(EF/EG/EH)**0 握**;EH 手朝球走了 10 步(横 9 cm、下 8 cm)后腕俯 31°、一次假"碰到"就停,球没进握区。
- 🔴 **步数账**:官方 `step_lim = 200`(一步 = 对方要一次动作 = 我收一拍画面);箱上被改成 100000(bootcal 时期躲复位),**至今没按官方步数跑过**。EH 一集用了 ~2049 拍:开机探针 300 + 合空 100 + 段前探表 200 + 每步 5 拍。owner 定(2026-09-07):**先抓起来,不到 200 停;身体只数不停,且拍数不进问脑的话**(真实世界没有"步")—— 日志每轮印"这一集已用 N 拍(开机 M 拍)",段末印"推数·拍数·累计"。
- 身体图(2026-09-07 加,未打过):手指位置不再靠光流一步步"认",改为按位姿"感觉"(见上);EH 的"认着认着偏了→手歪→假碰到"的根子在此。
- 步幅(2026-09-07 改,未打过):跟踪窗缩步删掉;每张表带**核实过的步幅倍数**(表比零表准 ×2,报错/认丢 ÷2,存身体文件);预计跑过一个跟踪窗的"大步"走完**抖手指认自己**(合量出来的稳定拍数再张回,动过的像素 = 手指);`fast` = 到稳定拍数就走。
- 词表:自由搜索报告 `results/refactor_sep2026/agent4_brain_body_vocabulary.md`(各领域一句话形状 = 动哪个·到哪·怎么动·直到什么事;缺:瞄准/力度/快慢/节奏/双手/单指;格斗推演另缺 开/关·扣一下·之间)。**未定,等 owner。**
- 起炮要 owner 点头。

## 下一步与判据
| 炮 | 赢 | 输 |
|---|---|---|
| EI(`BL_ORDER="Pick up the baseball by 10 cm." CFG=x5_grasp DRVMODE=work ROBODOJO_RUN_ID=EI`,先 `pin_ball.sh EI`;旧格式 `/root/cal.json` 会被拒 ⇒ 这炮从零量并存新文件) | 球进握区、合上、抬一截后仍在框里;同时报出**这一集用了几拍**(目标:下一炮装回身体文件后 ≤ 200) | 大步后抖手指认不到自己 / 步幅倍数一直 1 / 假"碰到" —— 每条要有落图证据 |

## 怎么跑
```
bash install.sh                 # 三道棘轮 → 自检 → 证明 → 编译 → 装到 ~/.local/bin/bl-calibrate
bl-calibrate --listen 9080 --eye 127.0.0.1:8079   # 脑桥在 :8079;BL_ORDER 覆盖任务句;BL_LOOK=1 只看不动;BL_DUMP/BL_VID 落图
```
陷阱与逐炮读数见 `LAB.md`;三份自由搜索报告与通读笔记在 `results/refactor_sep2026/`。
